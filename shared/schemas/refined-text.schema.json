{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "refined-text.schema.json",
  "title": "Refined Text",
  "description": "RefineText module output: corrected transcript text with start and end timestamps.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["start_time", "end_time", "text"],
    "additionalProperties": false,
    "properties": {
      "start_time": {
        "type": "number",
        "minimum": 0,
        "description": "Start time of the utterance in seconds. This value should be preserved from STT output."
      },
      "end_time": {
        "type": "number",
        "minimum": 0,
        "description": "End time of the utterance in seconds. This value should be preserved from STT output."
      },
      "text": {
        "type": "string",
        "minLength": 1,
        "description": "Corrected and refined text."
      }
    }
  }
}
