{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "refined-text.schema.json",
  "title": "Refined Text",
  "description": "RefineText module output: corrected transcript text with timestamp.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["time", "text"],
    "additionalProperties": false,
    "properties": {
      "time": {
        "type": "number",
        "minimum": 0,
        "description": "Start time of the utterance in seconds. This value should be preserved from STT output."
      },
      "text": {
        "type": "string",
        "minLength": 1,
        "description": "Corrected and refined text."
      }
    }
  }
}